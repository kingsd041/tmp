apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: simple-fanout-example
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: demo1.kingsd.top
    http:
      paths:
      - path: /
        backend:
          serviceName: my-service
          servicePort: 80
